# Worker base URL and the API_KEY secret it expects. Server-side only — never
# exposed to the browser (no NEXT_PUBLIC_ prefix).
TRACKER_URL=https://mail-tracker.krishnadobhal234.workers.dev
TRACKER_API_KEY=change-me
APP_PASSWORD=change-me-too
